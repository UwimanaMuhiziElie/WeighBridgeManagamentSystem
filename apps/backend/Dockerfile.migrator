# apps/backend/Dockerfile.migrator
FROM postgres:16-alpine

WORKDIR /app

# Copy migration script + migrations
COPY apps/backend/migrate.sh /app/migrate.sh
RUN chmod +x /app/migrate.sh

# Migrations will be mounted at /migrations in docker-compose OR copied in
# COPY apps/backend/migrations /migrations

CMD ["/app/migrate.sh"]
